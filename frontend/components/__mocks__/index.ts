/**
 * Component Mocks
 *
 * Centralized mock exports for all components that need mocking in tests.
 */

// FileTree mocks
export { MockFileTree } from "../editor/__mocks__/FileTree.mock"
export type { MockFileTreeProps as FileTreeMockProps } from "../editor/__mocks__/FileTree.mock"

// MonacoWrapper mocks
export { MockMonacoWrapper } from "../editor/__mocks__/MonacoWrapper.mock"
export type { MockMonacoWrapperProps as MonacoWrapperMockProps } from "../editor/__mocks__/MonacoWrapper.mock"
