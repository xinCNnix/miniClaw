# 知识库功能测试计划

## 测试概述

本文档详细说明知识库功能的测试计划、测试场景和验证步骤。

**测试范围**: 知识库后端 API、前端 UI 组件、E2E 功能测试
**测试日期**: 待定（代码已完成）
**测试环境**: 开发环境

---

## 测试环境准备

### 前置条件

1. **后端环境**
   ```bash
   cd backend
   pip install -r requirements.txt
   python -m uvicorn app.main:app --port 8002 --reload
   ```

2. **前端环境**
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

3. **依赖验证**
   - 后端所有依赖已安装（已完成 ✅）
   - 前端所有依赖已安装（已完成 ✅）
   - RAG 引擎可以成功导入（已验证 ✅）

### 测试数据准备

创建测试文档：

**txt 文档** (`test-document.txt`):
```
miniClaw 是一个轻量级的 AI Agent 系统。

主要特性：
1. 支持多个 LLM 提供商（OpenAI、DeepSeek、Qwen、Ollama 等）
2. 工具调用功能（文件操作、代码执行、终端命令等）
3. 知识库检索功能（RAG）
4. 会话管理

系统架构：
- 后端：Python + FastAPI + LangChain + LlamaIndex
- 前端：Next.js + TypeScript + Shadcn/UI
```

**Markdown 文档** (`test-guide.md`):
```markdown
# miniClaw 使用指南

## 快速开始

1. 启动后端服务
2. 启动前端服务
3. 开始对话

## 功能说明

### 文件操作
- 读取文件
- 写入文件
- 列出目录

### 代码执行
- Python REPL
- 终端命令
```

---

## 阶段 1：后端 API 测试

### 1.1 健康检查测试

**目的**: 验证后端服务正常启动

**步骤**:
```bash
curl http://localhost:8002/health
```

**预期结果**:
```json
{
  "status": "healthy",
  "app": "miniClaw",
  "version": "0.1.0",
  "llm_provider": "qwen",
  "agent_initialized": true
}
```

**验证点**:
- ✅ 服务正常响应
- ✅ llm_provider 正确显示
- ✅ agent_initialized 为 true

---

### 1.2 文档上传测试

#### 测试用例 1.2.1：上传 TXT 文档

**步骤**:
```bash
curl -X POST http://localhost:8002/api/kb/upload \
  -F "file=@test-document.txt"
```

**预期结果**:
```json
{
  "success": true,
  "document": {
    "id": "...",
    "filename": "test-document.txt",
    "file_type": ".txt",
    "size": 234,
    "upload_date": "2026-03-07T...",
    "chunk_count": 2
  },
  "message": "Document uploaded successfully: test-document.txt"
}
```

**验证点**:
- ✅ 返回 success: true
- ✅ 文档元数据正确
- ✅ chunk_count > 0
- ✅ 文件已保存到 `backend/data/knowledge_base/`

---

#### 测试用例 1.2.2：上传 Markdown 文档

**步骤**:
```bash
curl -X POST http://localhost:8002/api/kb/upload \
  -F "file=@test-guide.md"
```

**预期结果**: 同上，文件类型为 `.md`

---

#### 测试用例 1.2.3：上传 PDF 文档

**步骤**:
```bash
curl -X POST http://localhost:8002/api/kb/upload \
  -F "file=@test.pdf"
```

**预期结果**: 成功上传并索引

---

#### 测试用例 1.2.4：文件类型验证

**步骤**:
```bash
curl -X POST http://localhost:8002/api/kb/upload \
  -F "file=@test.exe"
```

**预期结果**:
```json
{
  "detail": "Unsupported file type: .exe. Supported: ['.txt', '.md', '.pdf', '.docx']"
}
```

**验证点**:
- ✅ 返回 400 错误
- ✅ 错误消息清晰

---

#### 测试用例 1.2.5：文件大小验证

**步骤**:
创建一个 >10MB 的文件并上传

**预期结果**:
```json
{
  "detail": "File too large: 11534336 > 10485760"
}
```

**验证点**:
- ✅ 返回 413 错误
- ✅ 文件被拒绝

---

### 1.3 文档列表测试

**步骤**:
```bash
curl http://localhost:8002/api/kb/documents
```

**预期结果**:
```json
{
  "documents": [
    {
      "id": "...",
      "filename": "test-document.txt",
      "file_type": ".txt",
      "size": 234,
      "upload_date": "2026-03-07T...",
      "chunk_count": 2
    }
  ],
  "total": 1
}
```

**验证点**:
- ✅ 显示所有上传的文档
- ✅ total 数量正确
- ✅ 元数据完整

---

### 1.4 文档删除测试

**步骤**:
```bash
curl -X DELETE http://localhost:8002/api/kb/documents/{doc_id}
```

**预期结果**:
```json
{
  "success": true,
  "message": "Document deleted successfully: test-document.txt"
}
```

**验证点**:
- ✅ 返回 success: true
- ✅ 文件从磁盘删除
- ✅ 文档从列表中移除

---

#### 测试用例 1.4.1：删除不存在的文档

**步骤**:
```bash
curl -X DELETE http://localhost:8002/api/kb/documents/invalid-id
```

**预期结果**:
```json
{
  "detail": "Document not found: invalid-id"
}
```

**验证点**:
- ✅ 返回 404 错误

---

### 1.5 统计信息测试

**步骤**:
```bash
curl http://localhost:8002/api/kb/stats
```

**预期结果**:
```json
{
  "total_documents": 2,
  "total_chunks": 5,
  "total_size": 1523,
  "last_updated": "2026-03-07T..."
}
```

**验证点**:
- ✅ 统计数据准确
- ✅ last_updated 时间正确

---

### 1.6 知识检索测试（search_kb 工具）

**步骤**: 通过聊天接口测试

```bash
curl -X POST http://localhost:8002/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "miniClaw 的主要特性有哪些？",
    "stream": false
  }'
```

**预期结果**: Agent 应该使用 search_kb 工具检索相关文档，并基于文档内容回答

**验证点**:
- ✅ Agent 调用了 search_kb 工具
- ✅ 返回相关文档内容
- ✅ 回答基于检索到的内容

---

## 阶段 2：前端 UI 测试

### 2.1 标签页切换测试

**目的**: 验证标签页切换功能

**步骤**:
1. 打开浏览器访问 `http://localhost:3000/chat`
2. 查看右侧面板
3. 点击 "Editor" 标签
4. 点击 "Knowledge Base" 标签

**预期结果**:
- ✅ 两个标签显示正确
- ✅ 点击标签后内容切换
- ✅ 激活标签有蓝色边框高亮
- ✅ 鼠标悬停有灰色背景

---

### 2.2 知识库面板展示测试

**目的**: 验证知识库面板正确显示

**步骤**:
1. 切换到 "Knowledge Base" 标签
2. 观察面板布局

**预期结果**:
- ✅ 显示标题 "Knowledge Base"
- ✅ 显示文档数量统计
- ✅ 显示拖拽上传区域
- ✅ 显示上传提示（支持的文件格式）
- ✅ 显示文档列表或空状态

---

### 2.3 拖拽上传测试

**目的**: 验证拖拽上传功能

**步骤**:
1. 准备一个测试文件（test.txt）
2. 拖拽文件到上传区域
3. 观察 UI 变化

**预期结果**:
- ✅ 拖拽时上传区域有视觉反馈
- ✅ 显示 "Uploading..." 状态
- ✅ 上传完成后刷新文档列表
- ✅ 文档出现在列表中
- ✅ 显示正确的文件元数据（名称、大小、chunks 数量、时间）

---

### 2.4 点击上传测试

**目的**: 验证点击上传功能

**步骤**:
1. 点击上传区域
2. 在文件选择器中选择文件
3. 观察 UI 变化

**预期结果**:
- ✅ 打开文件选择器
- ✅ 只显示允许的文件类型
- ✅ 选择文件后开始上传
- ✅ 上传完成后刷新列表

---

### 2.5 错误处理测试

#### 测试用例 2.5.1：不支持文件类型

**步骤**:
1. 尝试上传 .exe 文件

**预期结果**:
- ✅ 显示错误提示
- ✅ 错误消息：`Unsupported file type: .exe`
- ✅ 不影响后续操作

---

#### 测试用例 2.5.2：文件过大

**步骤**:
1. 尝试上传 >10MB 的文件

**预期结果**:
- ✅ 显示错误提示
- ✅ 错误消息：`File too large (max 10MB)`

---

#### 测试用例 2.5.3：网络错误

**步骤**:
1. 停止后端服务
2. 尝试上传文件

**预期结果**:
- ✅ 显示错误提示
- ✅ 错误消息：`Failed to upload document`
- ✅ 可以重试

---

### 2.6 文档列表测试

**目的**: 验证文档列表正确显示

**步骤**:
1. 上传多个文档
2. 查看文档列表

**预期结果**:
- ✅ 每个文档显示文件图标
- ✅ 显示文件名
- ✅ 显示文件大小（格式化：KB/MB）
- ✅ 显示 chunk 数量
- ✅ 显示上传时间
- ✅ 时间格式正确

---

### 2.7 文档删除测试

**目的**: 验证文档删除功能

**步骤**:
1. 点击文档的删除按钮（垃圾桶图标）
2. 在确认对话框中点击"确定"
3. 观察列表变化

**预期结果**:
- ✅ 显示确认对话框
- ✅ 确认后文档从列表消失
- ✅ 文档数量统计更新

---

#### 测试用例 2.7.1：取消删除

**步骤**:
1. 点击删除按钮
2. 在确认对话框中点击"取消"

**预期结果**:
- ✅ 文档保留在列表中
- ✅ 不显示错误

---

### 2.8 空状态测试

**目的**: 验证空状态显示

**步骤**:
1. 删除所有文档
2. 查看知识库面板

**预期结果**:
- ✅ 显示空状态图标
- ✅ 显示提示文字："No documents yet"
- ✅ 显示说明文字
- ✅ 上传区域仍然可用

---

## 阶段 3：E2E 功能测试

### 3.1 完整上传流程测试

**目的**: 验证从上传到检索的完整流程

**步骤**:
1. 创建测试文档（包含特定内容）
2. 通过前端上传文档
3. 等待上传完成
4. 在聊天框询问文档相关内容
5. 验证 Agent 回答

**测试文档内容**:
```
测试文档内容：
本项目名称是 miniClaw
版本号是 0.1.0
使用 FastAPI 和 Next.js 构建
```

**测试问题**:
- "项目名称是什么？" → 应回答 "miniClaw"
- "版本号是多少？" → 应回答 "0.1.0"
- "使用什么技术构建？" → 应提到 "FastAPI" 和 "Next.js"

**验证点**:
- ✅ 上传成功
- ✅ 文档索引成功
- ✅ Agent 检索到相关内容
- ✅ 回答基于文档内容
- ✅ 回答准确

---

### 3.2 多文档检索测试

**目的**: 验证多文档情况下的检索

**步骤**:
1. 上传文档 A（关于 Python）
2. 上传文档 B（关于 JavaScript）
3. 询问 Python 相关问题
4. 询问 JavaScript 相关问题

**验证点**:
- ✅ 两个文档都成功上传
- ✅ Python 问题检索到文档 A
- ✅ JavaScript 问题检索到文档 B
- ✅ 回答不会混淆

---

### 3.3 混合搜索测试

**目的**: 验证 BM25 + 向量混合搜索

**步骤**:
1. 上传包含特定关键词的文档
2. 使用关键词搜索
3. 使用语义搜索
4. 对比结果

**验证点**:
- ✅ 关键词匹配准确
- ✅ 语义理解正确
- ✅ 结果排序合理

---

### 3.4 Agent 集成测试

**目的**: 验证 Agent 正确使用 search_kb 工具

**测试场景**:
1. 上传技术文档
2. 询问技术问题
3. 观察 Agent 行为

**验证点**:
- ✅ Agent 调用 search_kb 工具
- ✅ 传递正确的查询参数
- ✅ 使用检索到的上下文
- ✅ 回答自然流畅

---

## 阶段 4：性能测试

### 4.1 大文件上传测试

**目的**: 测试大文件上传性能

**步骤**:
1. 准备接近 10MB 限制的文件
2. 上传文件
3. 记录上传时间

**验证点**:
- ✅ 上传成功
- ✅ 上传时间 < 30 秒
- ✅ 索引时间 < 60 秒
- ✅ UI 不会卡死

---

### 4.2 多文档并发测试

**目的**: 测试多文档同时存在时的性能

**步骤**:
1. 上传 10 个文档
2. 执行搜索查询
3. 记录响应时间

**验证点**:
- ✅ 所有文档成功索引
- ✅ 搜索响应时间 < 5 秒
- ✅ 内存使用正常

---

### 4.3 大规模文档测试

**目的**: 测试系统上限

**步骤**:
1. 上传 50+ 个文档
2. 执行搜索
3. 观察性能

**验证点**:
- ✅ 系统不崩溃
- ✅ 搜索仍然可用
- ✅ 响应时间可接受

---

## 测试检查清单

### 后端 API（17 个测试）

- [ ] 1.1 健康检查
- [ ] 1.2.1 上传 TXT 文档
- [ ] 1.2.2 上传 Markdown 文档
- [ ] 1.2.3 上传 PDF 文档
- [ ] 1.2.4 文件类型验证
- [ ] 1.2.5 文件大小验证
- [ ] 1.3 文档列表
- [ ] 1.4 文档删除
- [ ] 1.4.1 删除不存在的文档
- [ ] 1.5 统计信息
- [ ] 1.6 知识检索（search_kb）

### 前端 UI（15 个测试）

- [ ] 2.1 标签页切换
- [ ] 2.2 知识库面板展示
- [ ] 2.3 拖拽上传
- [ ] 2.4 点击上传
- [ ] 2.5.1 不支持文件类型错误
- [ ] 2.5.2 文件过大错误
- [ ] 2.5.3 网络错误
- [ ] 2.6 文档列表显示
- [ ] 2.7 文档删除
- [ ] 2.7.1 取消删除
- [ ] 2.8 空状态显示

### E2E 功能（4 个测试）

- [ ] 3.1 完整上传流程
- [ ] 3.2 多文档检索
- [ ] 3.3 混合搜索
- [ ] 3.4 Agent 集成

### 性能测试（3 个测试）

- [ ] 4.1 大文件上传
- [ ] 4.2 多文档并发
- [ ] 4.3 大规模文档

---

## 测试执行顺序

### 第 1 天：基础功能测试

1. 后端 API 测试（1.1 - 1.6）
2. 前端 UI 测试（2.1 - 2.8）

### 第 2 天：E2E 测试

3. E2E 功能测试（3.1 - 3.4）
4. 性能测试（4.1 - 4.3）

---

## 测试工具

### 手动测试工具

- **浏览器**: Chrome/Edge
- **curl**: API 测试
- **Postman**: API 测试（可选）

### 自动化测试（可选）

```bash
# 后端单元测试
cd backend
pytest tests/test_rag_engine.py -v

# 前端组件测试
cd frontend
npm test knowledge-base-panel.test.tsx

# E2E 测试
cd frontend
npx playwright test knowledge-base
```

---

## 测试报告模板

### 测试结果汇总

| 测试类别 | 总数 | 通过 | 失败 | 通过率 |
|---------|------|------|------|--------|
| 后端 API | 11 | - | - | -% |
| 前端 UI | 10 | - | - | -% |
| E2E 功能 | 4 | - | - | -% |
| 性能测试 | 3 | - | - | -% |
| **总计** | **28** | - | - | **-%** |

### 缺陷列表

| ID | 严重程度 | 描述 | 状态 |
|----|---------|------|------|
| BUG-001 | - | - | - |

---

## 验收标准

### 必须满足（P0）

- ✅ 所有后端 API 测试通过
- ✅ 所有前端 UI 测试通过
- ✅ 至少 1 个完整的 E2E 测试通过
- ✅ 无严重（P0）缺陷

### 应该满足（P1）

- ✅ 所有 E2E 测试通过
- ✅ 性能测试基本通过
- ✅ 无高级（P1）缺陷

### 可以满足（P2）

- ✅ 性能测试全部通过
- ✅ 用户体验优化

---

## 注意事项

1. **测试环境**: 使用开发环境测试，不要使用生产环境
2. **测试数据**: 使用测试文档，不要使用敏感数据
3. **错误截图**: 失败测试需要截图保存
4. **日志记录**: 记录测试过程中的异常
5. **版本控制**: 测试过程中不要修改代码（除非发现 bug）

---

## 回归测试

修复 bug 后需要执行的回归测试：

1. 重新执行失败的测试用例
2. 执行相关的测试用例
3. 执行完整的 E2E 测试
4. 验证没有引入新的 bug

---

**测试负责人**:
**测试日期**:
**文档版本**: 1.0
